Algoritmo "soma"

Var

  x, y, soma : inteiro

Inicio

   escreval("digite um numero de x:")
   leia(x)
   escreval ("digite um numero de y:")
   leia(y)
   
   soma <- x + y
   escreval ("soma=", soma)


Fimalgoritmo